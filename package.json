{
  "name": "typescript-users-and-posts",
  "version": "1.0.0",
  "description": "TypeScript exercise with users and posts data from DummyJSON",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "start": "node dist/usersAndPosts.js",
    "test": "vitest run"
  },
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
